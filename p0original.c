#include<stdio.h>

float find_largest(int a,int x,int y,int z)
{
  char a;
  if(x>y){
    (x>z)
  {
    x=a;
  }
  else if(y>x) and (y>z)
  {
   y=a; 
  }
  else if(z>x) and (z>y)
    {
    z=a;
    }
  return a;
}
int main(void)
{
  float a,x,y,z;
  printf("enter the three numbers:\n");
  scanf("%f%f%f", &x,&y,&z);
  float a=find_largest(x,y,z);
  printf("the largest number among %f,%f and %f is:\n",x,y,z);
  return 0;
}
  